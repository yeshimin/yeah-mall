<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeshimin.yeahboot.data.mapper.OrderMapper">

    <!-- 更新订单状态 -->
    <update id="updateOrderStatus">
        update s_order set status = #{statusTo} where id = #{id} and status = #{statusFrom} and deleted = 0
    </update>

    <!-- 更新退款状态 -->
    <update id="updateRefundStatus">
        update s_order set refund_status = #{statusTo} where id = #{id} and refund_status = #{statusFrom} and deleted = 0
    </update>

</mapper>
